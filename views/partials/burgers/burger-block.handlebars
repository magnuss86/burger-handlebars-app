<li>
	{{burger_name}}

	<button class="change-devour bt-color" data-id="{{id}}" data-newdevour="{{devour}}">
		{{#if devour}}LUNCH TIME!{{else}}I want that again!!!{{/if}}
	</button>

	<button class="delete-burger bt-color" data-id="{{this.id}}">DELETE!</button>
</li>


